# Gemini Placeholder

Gemini support for `pet-companion` is planned but not documented in this first public package version.

When Gemini guidance is added, keep it aligned with the shared schemas in `core/` and the package-level viewer examples.
