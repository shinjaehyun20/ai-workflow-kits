# Pet Companion Instructions For GitHub Copilot

Use this package when the repository needs a public-safe companion workflow instead of a private local pet bundle.

## Rules

- Treat the shared JSON schemas as the source of truth.
- Prefer external viewer or webview guidance over native pet assumptions.
- Keep Copilot-specific notes inside this package instead of copying Codex configuration.
- Verify examples with file paths and schema checks before finalizing.

## Final Response

Include:

- changed files
- package or example path
- checks run
- unsupported runtime behavior
