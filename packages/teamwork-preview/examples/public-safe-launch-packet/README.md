# Public-Safe Launch Packet Example

This example shows how to prepare a multi-agent launch packet for a documentation migration.

Scenario:

- A repository has old setup docs and new runtime docs.
- The user wants independent workers to inspect each area.
- The main session must decide what gets merged and verify the final result.

Open `prompt_draft.example.md` for the launch packet.
