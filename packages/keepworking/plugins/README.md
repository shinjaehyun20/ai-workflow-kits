# Keepworking Plugins

Optional plugins can live here when the workflow needs executable extensions such as dashboards, registry viewers, or validators.

Keep plugins separate from runtime adapters:

- `codex/`, `claude/`, `gemini/`, `copilot/` translate workflow behavior.
- `plugins/` contains optional executable extensions.
